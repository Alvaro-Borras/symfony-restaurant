{% extends 'base.html.twig' %}
      {% block stylesheet %}
      <link rel="icon" href="{{ asset('styles/logo-restaurant.svg') }}">
      <link rel='stylesheet' href="{{asset('styles/css/bootstrap.min.css')}}"/>
      {# <link rel='stylesheet' href="{{asset('styles/css/bootstrap-icons.css')}}"/> #}
      <style>
        h1, #a{
            padding-top: 50px;
            height: 150px;
            text-decoration:underline;
            font-weight:bold;
            background: linear-gradient(135deg, orange 30%, rgb(200, 222, 60));
            text-decoration: none;
            color: #000;
        }
        
        body{
            background-color: blanchedalmond;
        }

        input[type="date"]{
        width: 300px;
        }
        #footer{
            background: linear-gradient(217deg, rgba(195, 72, 72, 0.8), rgba(255,0,0,0) 70.71%),
            linear-gradient(127deg, rgba(224, 216, 101, 0.8), rgba(0,255,0,0) 70.71%),
            linear-gradient(336deg, rgba(65, 105, 10, 0.67), rgba(0,0,255,0) 70.71%);
            height: 150px;
            text-align: center;
            font-size: 20px;
            margin-top: 320px;
        }
        #arreglo{
            padding: 50px 0 0 0;
            margin: auto;
          
        }
      </style>
      
{% endblock %}
