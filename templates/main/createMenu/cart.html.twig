{% extends 'base.html.twig' %}

{% block title %}Restaurant 🍴{% endblock %}   
{% block stylesheet %}
 {{ include('includes/header.html.twig') }}
 {% endblock %}
{% block body %}
    <a id="a" href="/"><h1 class="m-0 row justify-content-center">TU CARRO</h1></a>
   <center> 
  <div class="container align-items-center d-flex flex-column justify-content-center rounded my-5 w-50">
   <table class="table table-hover table-fixed">
        <thead>
          <tr>
            {# <th>Linea</th>#}
            <th>Descripción</th> 
            <th>Categoria</th>
            <th>Precio</th>
          </tr>
        </thead>
        <tbody>
        {% for cat in vec %}       
          <tr>      
            <td>{{cat.item}}</td> 
            <td>{{cat.category}}</td>
            <td>{{cat.price}}</td>
            {# <td><a href="/delete/{{cat.getIdF()}}"><input class="btn btn-warning" type="button" value="Eliminar"></a></td>
            <td><a href="/modifyItem/{{cat.getIdF()}}"><input class="btn btn-warning" type="button" value="Modificar"></a></td>  #}
          </tr>
			  {% endfor %}       
        </tbody>
    </table>
  </div>

      <p><a href="/menu" class="btn btn-warning">Continua tu orden</a> o 
      <a href="/delete"><input type="button" class="btn btn-warning" value="Carro vacio"> </p>
      {# <input class="btn btn-warning" type="button" value="Terminar Ticket"> #}
      <br>
      <br>
      <a href="/"><input class="btn btn-warning" type="button" value="Página Principal"></a>
    </center>
    <footer>
      <div id="footer">
          <p id="arreglo">AMA Company S.A</p>
      </div>
    </footer>
{% endblock %}
{% block javascripts %}
{# <script src="{{asset('styles/js/jquery.js')}}"></script> #}
<script src="{{asset('styles/js/bootstrap.min.js')}}"></script>
{% endblock %}
